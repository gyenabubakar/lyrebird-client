{
  "name": "lyrebird",
  "version": "0.0.0",
  "private": false,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "prettier": "prettier --write '**/*.{ts,tsx,md,json,vue}'"
  },
  "devDependencies": {
    "prettier": "2.8.0",
    "turbo": "*"
  },
  "engines": {
    "node": ">=16.x"
  },
  "packageManager": "npm@8.15.0"
}
